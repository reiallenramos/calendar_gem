<%= week_calendar events: @meetings, number_of_weeks: 2 do |date, meetings| %>
  <div>
    <div> 
      <%= date.day %>
    </div>
      <% meetings.each do |meeting| %>
          <div>
            <%= link_to meeting.name, meeting %>
          </div>
      <% end %>
  </div>
<% end %>